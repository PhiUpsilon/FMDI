# Locally generated result artifacts

This directory is reserved for summaries generated by the public analysis
interface. The repository does not distribute numerical experiment results,
logs, model weights, or generated prediction tensors.

After running experiments under `outputs/`, aggregate metric files with:

```bash
python -m fmdi.analysis outputs --output results/generated/aggregated_results.csv
```

Generated files remain untracked by default.
