# Main-comparison experiment. Replace defaults with paper settings.
experiment:
  name: main
  seed: 2026
  output_dir: results/metrics/main

data:
  dataset: TODO
  path: data/processed

model:
  name: fmdi
  parameters: {}

evaluation:
  metrics: [TODO]
